<!DOCTYPE html>
<html>
  <head>
    <title>Instachat</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag 'post', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_importmap_tags %>
  </head>
  <body>
    <%# <%= image_tag 'bird.svg', alt: 'Instachat Logo', class: 'logo' %>
    <header>
      <h1>
        Instachat
      </h1>
      <%= yield :top_links %>
    </header>
    <div class="flash">
      <% flash.each do |key, value|%>
        <h2 class=<%= key %> <%= value %></h2>
        <% end %>
      </div>
      <main class="main_layout">
        <div class="navigation_main">
          <ul class="links">
            <li> <%= link_to "New Posts", posts_path%></li>
            <li> <%= link_to "Users", users_path%></li>
            <li> <%= link_to "Post something.", new_post_path %></li>
            <% unless current_user.nil? then %>
              <li> <%= link_to "Your Profile", current_user %></li>
              <li><%= link_to "Sign out", destroy_user_session_path, data: { turbo_method: :delete } %></li>
            <% end %>
          </ul>
        </div>
        <%= yield %>
      </main>
    </body>
  </html>
